
!!! Если загружать все данные таблиц бд в память прогрммы, то запросы к бд ограничатся примитивными :
    - insert, delete, update сущности без условий
    - select всё c возможными джойнами(которые делают крайне сложными конструирование утилитарных классов запросов)
Поэтому нет смысла и это сложно из-за джойнов делать отдельные утилитарные классы для запросов sql и
можно разместить все запросы в табличные классы и объект соединения к бд.
Поэтому дальнейшую разаботку этого паттерна остнавливаю.

2) посмотреть нужен ли enum полей в таблице
3) подумать как и где лучше мэппировать поля таблицы и значения для сущности
4) сделать сервис-манагер, который будет содержать объект дб(connect), dcl, и объекты таблиц
    и избавиться от статики.
5) разбить dcl на отдельные объекты: update (insert, update, delete), select, expression(для where)
6) Хранить все записи таблицы бд в сете сущностей(Set<entity>) в шлюзовой таблице.

